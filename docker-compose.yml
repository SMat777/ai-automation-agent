services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    depends_on:
      - pipeline
    volumes:
      - ./output:/app/output

  pipeline:
    build:
      context: ./automation
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
